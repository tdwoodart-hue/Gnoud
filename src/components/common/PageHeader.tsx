import React from 'react';
export const PageHeader: React.FC<{ title: string; meta?: string; action?: React.ReactNode }> = ({ title, meta, action }) => <header className="mb-5 flex h-16 items-center justify-between gap-4 border-b border-slate-200"><div className="flex min-w-0 items-baseline gap-3"><h1 className="truncate text-2xl font-bold tracking-tight text-slate-950">{title}</h1>{meta && <p className="shrink-0 text-xs font-medium text-slate-400">{meta}</p>}</div><div className="flex h-10 w-10 shrink-0 items-center justify-center">{action}</div></header>;
